import * as React from 'react'
import { View, Text, Images, SafeAreaView } from 'react-native'

const Header = () => {
    return (
        <SafeAreaView style={{ backgroundColor: 'orange', height: 100 }}>
            <Text>This is header</Text>
        </SafeAreaView>
    )
}

export default Header