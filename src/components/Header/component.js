import * as React from 'react'
import { View, Text, Images } from 'react-native'

const Header = () => {
    return (
        <View style={{ height: 60, backgroundColor: 'orange', paddingTop: 100 }}>
            <Text>This is header</Text>
        </View>
    )
}

export default Header